# STM32-Exercises
```
git add 单个文件
git add 文件夹1/ 文件夹2/ ……多个文件夹之间空格隔开
git add .

git commit -m "注释"

git push -u origin master

103_0同步：
git add 103_0
git commit -m "日常编辑同步"
git push -u origin main

readme同步：
git add README.md
git commit -m "日常编辑同步"
git push -u origin main
